{
  "name": "mavericks-execution-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Repository command hub for the Mavericks Execution Platform applications.",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "test": "npm --prefix client run test",
    "test:server": "npm --prefix server run test",
    "test:functions": "npm --prefix azure-functions run test",
    "validate:prisma": "cd server && npx prisma validate",
    "verify": "npm run test && npm run build && npm run test:server && npm run validate:prisma && npm run test:functions"
  }
}
